{
  "name": "triviabackend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "reset-db": "dropdb triviaApp; createdb triviaApp; psql -f schema.sql triviaApp; psql -f seed.sql triviaApp"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^6.2.0",
    "pg-promise": "^8.5.5"
  }
}
